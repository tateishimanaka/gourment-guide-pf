<div class="container">
  <div class="row">
    <div class="col-md-10">
      <h2>店舗一覧</h2>
      <% @shops.each do |shop| %>
        <%= shop.shop_name %>
        <%= shop.shop_name_kana %>
      <% end %>
    </div>
  </div>
</div>